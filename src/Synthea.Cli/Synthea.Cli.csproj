<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <PackAsTool>true</PackAsTool>
    <PackageId>synthea-cli</PackageId>
    <ToolCommandName>synthea</ToolCommandName>
    <DotNetCliToolAliases>syn</DotNetCliToolAliases>
    <!-- Single version source: drives PackageVersion, AssemblyVersion, and the
         InformationalVersion the CLI reports for its version flag. CI passes
         /p:Version=<tag> at pack time (see .github/workflows/nuget.yml). -->
    <Version>1.0.3</Version>
    <!-- Keep the reported version a clean semver (no +<git-sha>) and identical
         across build contexts; the Docker build has no .git to read a sha from. -->
    <IncludeSourceRevisionInInformationalVersion>false</IncludeSourceRevisionInInformationalVersion>
    <Description>CLI wrapper around MITRE Synthea synthetic patient generator. Downloads the latest Synthea release, manages a local JAR cache, and exposes Synthea's generation flags as first-class .NET CLI options.</Description>
    <!-- Tags drive NuGet search relevance. Order matters: most-specific
         to most-general so query tokenization favors the right hits. -->
    <PackageTags>synthea;synthetic-data;fhir;hl7;ehr;healthcare;health-data;cli;dotnet-tool</PackageTags>
    <PackageLicenseFile>LICENSE</PackageLicenseFile>
    <PackageReadmeFile>README.md</PackageReadmeFile>
    <PackageIcon>icon.png</PackageIcon>
    <PackageProjectUrl>https://github.com/Kmanley1/SyntheaCli</PackageProjectUrl>
    <RepositoryUrl>https://github.com/Kmanley1/SyntheaCli</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
    <!-- Publish symbols as a .snupkg alongside the .nupkg so step-into
         debugging works for downstream consumers. dotnet nuget push of the
         .nupkg automatically pushes the matching .snupkg from the same folder. -->
    <IncludeSymbols>true</IncludeSymbols>
    <SymbolPackageFormat>snupkg</SymbolPackageFormat>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.DependencyInjection" Version="10.0.10" />
    <PackageReference Include="Microsoft.Extensions.Logging" Version="10.0.10" />
    <PackageReference Include="Microsoft.Extensions.Logging.Console" Version="10.0.8" />
    <PackageReference Include="System.CommandLine" Version="2.0.8" />
  </ItemGroup>

  <ItemGroup>
    <!-- csproj lives in src/Synthea.Cli/, so the repo root is two levels up. -->
    <None Include="../../LICENSE" Pack="true" PackagePath="" />
    <None Include="../../README.md" Pack="true" PackagePath="" />
    <!-- 128x128 PNG sourced next to the csproj; nuget.org renders it as
         the package thumbnail in search results and on the detail page. -->
    <None Include="icon.png" Pack="true" PackagePath="" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="Synthea.Cli.UnitTests" />
    <InternalsVisibleTo Include="Synthea.Cli.IntegrationTests" />
  </ItemGroup>

</Project>
